$mainMod = SUPER # Sets "Windows" key as main modifier
$shiftMod = SUPER_SHIFT # Super+shift

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, grave, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, Escape, exec, pkill wlogout || wlogout
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, F, togglefloating,
bind = $mainMod, space, exec, pkill wofi || $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, U, togglesplit, # dwindle
bind = $mainMod, G, exec, kitty \
  --title gazelle \
  --override font_size=10 \
  -e gazelle

bind = $mainMod, V, exec, pkill wofi || cliphist list | wofi --dmenu --prompt "Search Clipboard..." | cliphist decode | wl-copy
bind = SUPER SHIFT, V, exec, cliphist wipe && notify-send "Clipboard Cleared"

bind = $shiftMod, S, exec, export HYPRSHOT_DIR="$HOME/Pictures/screenshots" && ~/.local/bin/hyprshot -m window 
bind = $mainMod, S, exec, export HYPRSHOT_DIR="$HOME/Pictures/screenshots" && ~/.local/bin/hyprshot -m region
# bind = , PRINT, exec, export HYPRSHOT_DIR="$HOME/Pictures/screenshots" && ~/.local/bin/hyprshot -m region
#bind = $mainMod, N, exec, bash -c 'if [ "$NOTIFICATION_TOGGLE_STATE" = "0" ]; then swaync-client -op; export NOTIFICATION_TOGGLE_STATE=1; else swaync-client -cp; export NOTIFICATION_TOGGLE_STATE=0; fi'
bind = $mainMod, N, exec, swaync-client -op

# hyprswitch keybinds
# exec-once = hyprswitch init --show-title --size-factor 5.5 --workspaces-per-row 5 &

# $key = W
# $mod = super
# bind = $mod, $key, exec, hyprswitch gui --mod-key $mod --key $key --max-switch-offset 9 --hide-active-window-border

# waybar toggle
bind = $mainMod, T, exec, ~/.config/hypr/scripts/toggle_waybar.sh

# animations toggle 
bind = $mainMod, A, exec, ~/.config/hypr/scripts/toggle-animations.sh

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

bind = $shiftMod, H, movewindow, l   # move left
bind = $shiftMod, L, movewindow, r   # move right
bind = $shiftMod, K, movewindow, u   # move up
bind = $shiftMod, J, movewindow, d   # move down

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, Tab, togglespecialworkspace, *
bind = $mainMod SHIFT, Tab, movetoworkspace, special:*

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s +5%
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
